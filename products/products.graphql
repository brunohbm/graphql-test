type Query {
    products: [Product]
    product(id: ID!): Product
    productsByPrice(min: Float!, max: Float!): [Product]
}

type Product {
    id: ID!
    price: Float!
    reviews: [Review]
    description: String!
}

type Review {
    rating: Int!
    comment: String
}
